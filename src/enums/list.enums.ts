enum NextPrev {
  Next = 'Next',
  Prev = 'Prev',
}

enum TableCellStyle {
  default = 'default',
  bold = 'bold',
}

enum TableCellType {
  Image = 'image',
  Text = 'text',
  Toggle = 'toggle',
}

enum UnitOfMeasure {
  inches = 'in',
  feet = 'ft',
  pounds = 'lb',
  millimeters = 'mm',
  meters = 'm',
  percent = '%',
}

export { NextPrev, TableCellStyle, TableCellType, UnitOfMeasure };
